import { defineComponent, h } from "vue";

export default defineComponent({
  name: "JSXButton",
  render() {
    return (
      <div>
        <button>JSX Button</button>
      </div>
    );
  },
});
